'use strict'

// global object for all structures and UI elements in the project

const globalAR = {

    // global structures

    scene: document.querySelector('a-scene'),
    marker: document.querySelector('#marker'),

    // scene elements

    tramModel: document.querySelector('.tramModel'),
}